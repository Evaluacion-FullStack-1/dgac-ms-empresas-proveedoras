spring.application.name=dgac-ms-empresas-proveedoras

# Puerto dinámico para la nube, y 8085 por defecto en tu entorno local
server.port=${PORT:8085}

# Conexión a la base de datos de Neon parametrizada
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-small-bar-apgh8exn-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_4ZyN7qikuaoA}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect