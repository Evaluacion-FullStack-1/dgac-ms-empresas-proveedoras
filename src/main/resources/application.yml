server:
  port: ${PORT:8085}

spring:
  application:
    name: dgac-ms-empresas-proveedoras
    
  # Conexión a la base de datos de Neon parametrizada
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-small-bar-apgh8exn-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_4ZyN7qikuaoA}
    driver-class-name: org.postgresql.Driver
    
  # Configuración de JPA / Hibernate
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true

# ==============================================================
# CONFIGURACIÓN DE INTEGRACIÓN CON OTROS MICROSERVICIOS
# ==============================================================
# URL base para comunicarse con el MS de Seguros
seguros:
  base-url: http://DGAC-MS-SEGUROS

# ==============================================================
# CONFIGURACIÓN DE EUREKA (Indispensable para el ecosistema)
# ==============================================================
eureka:
  instance:
    hostname: localhost
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8080/eureka/}